# GoalMap AI Server

Backend server for GoalMap AI, built with FastAPI, LangGraph, and uv.

## Development

1. Install `uv`: `curl -LsSf https://astral.sh/uv/install.sh | sh`
2. Run server:
   ```bash
   uv run uvicorn app.main:app --reload
   ```
